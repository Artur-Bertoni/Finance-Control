version: '3.1'

services:
  postgres:
    container_name: postgres
    image: postgres
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=finance-control-prd
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=pass