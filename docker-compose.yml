version: '3.1'

services:
  postgres-prod:
    container_name: postgres
    image: postgres
    restart: always
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=finance-control
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=pass